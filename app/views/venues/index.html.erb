<div id="venues_index">
	<h1>Venues</h1>
  <ul>
    <% for venue in @venues %>
      <li>
        <%= link_to(venue.title, venue_url(venue)) %>
        <% if venue.has_full_address? %>
          at <%= venue.full_address %>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>
